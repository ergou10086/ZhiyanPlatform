# 主配置文件—通用配置

spring:
  application:
    name: zhiyan-ai-dify

  profiles:
    # 默认使用dev环境，可通过环境变量覆盖
    active: prod

  # TODO 方案三优化：取消下面配置的注释以优化流式响应性能
  # webflux:
  #   # 禁用缓冲，支持真正的流式响应
  #   buffer-size: 512  # 使用小缓冲区
  #   max-header-size: 8KB
  # 
  # netty:
  #   # 禁用自动刷新缓冲
  #   connection-timeout: 60000

# 通用日志配置
logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n"