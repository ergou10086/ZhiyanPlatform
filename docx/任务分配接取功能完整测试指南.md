# 任务分配/接取功能task_user表集成 - 完整测试指南

## 📋 问题与解决方案

### 问题描述
用户反馈：**分配或接取任务后，没有将记录同步到task_user表中**

### 根本原因
TaskServiceImpl中的以下方法仍在使用旧的JSON方式存储assigneeId：
- ❌ `createTask()` - 创建任务
- ❌ `assignTask()` - 分配任务
- ❌ `claimTask()` - 接取任务
- ❌ `convertToDetailDTO()` - 查询任务详情
- ❌ `convertListToDetailDTO()` - 批量查询任务

### 修复内容
✅ **已完成的修改**：

| 修改项 | 文件 | 变更内容 |
|--------|------|---------|
| 1 | TaskServiceImpl.java | 添加TaskUserRepository依赖 |
| 2 | TaskServiceImpl.java | 添加必要的import (TaskUser, AssignType, RoleType等) |
| 3 | TaskServiceImpl.java | 更新createTask() - 写入task_user表 |
| 4 | TaskServiceImpl.java | 更新assignTask() - 软删除旧记录+添加新记录 |
| 5 | TaskServiceImpl.java | 更新claimTask() - 创建CLAIMED类型记录 |
| 6 | TaskServiceImpl.java | 更新convertToDetailDTO() - 从task_user表读取 |
| 7 | TaskServiceImpl.java | 更新convertListToDetailDTO() - 批量读取优化 |
| 8 | TaskServiceImpl.java | 更新convertToDetailDTOWithCache() - 缓存优化 |
| 9 | TaskDetailDTO.java | TaskAssigneeDTO添加assignType和assignedAt字段 |

---

## 🧪 测试验证步骤

### 前置条件
1. ✅ 数据库已执行迁移脚本：`V2.1__create_task_user_table.sql`
2. ✅ task_user表已创建
3. ✅ 项目已重新编译并启动

---

## 测试场景1：创建任务并分配执行者

### 1.1 发送请求

```bash
POST /api/tasks
Content-Type: application/json
Authorization: Bearer {token}

{
    "projectId": 123,
    "title": "测试任务-创建并分配",
    "description": "测试task_user表同步",
    "priority": "HIGH",
    "assigneeIds": [1001, 1002],
    "dueDate": "2025-12-31",
    "worktime": 8.5
}
```

### 1.2 验证数据库

```sql
-- 验证tasks表
SELECT id, title, assignee_id, status 
FROM tasks 
WHERE title = '测试任务-创建并分配';
-- 期望: assignee_id = '[]' (废弃字段)

-- 验证task_user表
SELECT 
    task_id,
    user_id,
    assign_type,
    assigned_by,
    is_active,
    role_type,
    assigned_at
FROM task_user 
WHERE task_id = {上面查到的任务ID}
  AND is_active = TRUE;

-- 期望结果：
-- | task_id | user_id | assign_type | assigned_by | is_active | role_type |
-- |---------|---------|-------------|-------------|-----------|-----------|
-- | 123     | 1001    | ASSIGNED    | 当前用户ID   | TRUE      | EXECUTOR  |
-- | 123     | 1002    | ASSIGNED    | 当前用户ID   | TRUE      | EXECUTOR  |
```

### 1.3 验证API响应

```bash
GET /api/tasks/{taskId}
```

**期望响应**：
```json
{
    "code": 200,
    "data": {
        "id": "123",
        "title": "测试任务-创建并分配",
        "assignees": [
            {
                "userId": "1001",
                "userName": "张三",
                "assignType": "ASSIGNED",
                "assignedAt": "2025-11-04T10:30:00Z"
            },
            {
                "userId": "1002",
                "userName": "李四",
                "assignType": "ASSIGNED",
                "assignedAt": "2025-11-04T10:30:00Z"
            }
        ]
    }
}
```

---

## 测试场景2：重新分配任务（软删除+新增）

### 2.1 发送请求

```bash
PUT /api/tasks/{taskId}/assign
Content-Type: application/json
Authorization: Bearer {token}

{
    "assigneeIds": [1003, 1004]
}
```

### 2.2 验证数据库

```sql
-- 验证旧执行者被软删除
SELECT 
    task_id,
    user_id,
    assign_type,
    is_active,
    removed_at,
    removed_by
FROM task_user 
WHERE task_id = {任务ID}
  AND user_id IN (1001, 1002);

-- 期望结果：
-- | task_id | user_id | assign_type | is_active | removed_at          | removed_by |
-- |---------|---------|-------------|-----------|---------------------|------------|
-- | 123     | 1001    | ASSIGNED    | FALSE     | 2025-11-04 11:00:00 | 操作人ID    |
-- | 123     | 1002    | ASSIGNED    | FALSE     | 2025-11-04 11:00:00 | 操作人ID    |

-- 验证新执行者被添加
SELECT 
    task_id,
    user_id,
    assign_type,
    assigned_by,
    is_active,
    assigned_at
FROM task_user 
WHERE task_id = {任务ID}
  AND user_id IN (1003, 1004)
  AND is_active = TRUE;

-- 期望结果：
-- | task_id | user_id | assign_type | assigned_by | is_active | assigned_at         |
-- |---------|---------|-------------|-------------|-----------|---------------------|
-- | 123     | 1003    | ASSIGNED    | 操作人ID     | TRUE      | 2025-11-04 11:00:00 |
-- | 123     | 1004    | ASSIGNED    | 操作人ID     | TRUE      | 2025-11-04 11:00:00 |

-- 验证历史记录保留
SELECT COUNT(*) as total_records 
FROM task_user 
WHERE task_id = {任务ID};
-- 期望: 4 (2条软删除 + 2条有效)
```

### 2.3 验证任务状态自动更新

```sql
-- 验证任务状态
SELECT status FROM tasks WHERE id = {任务ID};
-- 如果原本是TODO，期望自动变为IN_PROGRESS
```

---

## 测试场景3：用户主动接取任务

### 3.1 发送请求

```bash
POST /api/tasks/{taskId}/claim
Authorization: Bearer {token}
```

### 3.2 验证数据库

```sql
-- 验证新的执行者记录
SELECT 
    task_id,
    user_id,
    assign_type,
    assigned_by,
    is_active,
    assigned_at
FROM task_user 
WHERE task_id = {任务ID}
  AND user_id = {当前用户ID}
  AND is_active = TRUE;

-- 期望结果：
-- | task_id | user_id | assign_type | assigned_by | is_active | assigned_at         |
-- |---------|---------|-------------|-------------|-----------|---------------------|
-- | 123     | 1005    | CLAIMED     | 1005        | TRUE      | 2025-11-04 11:30:00 |

-- ✅ 关键验证点：
-- 1. assign_type = 'CLAIMED' (而不是ASSIGNED)
-- 2. assigned_by = user_id (自己分配给自己)
```

### 3.3 验证重复接取被阻止

```bash
POST /api/tasks/{taskId}/claim
Authorization: Bearer {token}
```

**期望响应**：
```json
{
    "code": 400,
    "message": "您已经是该任务的执行者"
}
```

---

## 测试场景4：查询用户的任务列表

### 4.1 使用新的查询方法

```bash
GET /api/tasks/my?page=0&size=10
Authorization: Bearer {token}
```

### 4.2 验证SQL查询

```sql
-- 后端应该使用这个查询
SELECT tu.*, t.* 
FROM task_user tu
INNER JOIN tasks t ON tu.task_id = t.id
WHERE tu.user_id = {当前用户ID}
  AND tu.is_active = TRUE
  AND t.is_deleted = FALSE
ORDER BY tu.assigned_at DESC;
```

### 4.3 验证响应数据

**期望响应**：
```json
{
    "code": 200,
    "data": {
        "content": [
            {
                "id": "123",
                "title": "我接取的任务",
                "assignees": [
                    {
                        "userId": "1005",
                        "userName": "当前用户",
                        "assignType": "CLAIMED",
                        "assignedAt": "2025-11-04T11:30:00Z"
                    }
                ]
            }
        ],
        "totalElements": 1
    }
}
```

---

## 测试场景5：批量查询任务（性能测试）

### 5.1 发送请求

```bash
GET /api/projects/{projectId}/tasks?page=0&size=50
```

### 5.2 验证SQL性能

使用`EXPLAIN ANALYZE`验证查询计划：

```sql
EXPLAIN ANALYZE
SELECT tu.* 
FROM task_user tu
WHERE tu.task_id IN (123, 124, 125, ...)  -- 最多50个ID
  AND tu.is_active = TRUE
ORDER BY tu.task_id, tu.assigned_at;
```

**期望**：
- ✅ 使用索引`idx_task_user_active`
- ✅ 查询时间 < 50ms
- ✅ 只有1次SQL查询（批量查询，而非N+1）

### 5.3 验证N+1问题已解决

查看日志，确认只有以下SQL：
```
1. 查询tasks表 (1次)
2. 批量查询task_user表 (1次)
3. 批量查询用户信息 (1次)
```

**不应该出现**：
❌ 每个任务查询一次task_user (N次)

---

## 测试场景6：数据一致性测试

### 6.1 同时分配+接取

```bash
# 步骤1：管理员分配用户A
PUT /api/tasks/{taskId}/assign
{"assigneeIds": [1001]}

# 步骤2：用户B主动接取
POST /api/tasks/{taskId}/claim
Authorization: Bearer {token_of_userB}

# 步骤3：验证数据库
SELECT user_id, assign_type FROM task_user 
WHERE task_id = {taskId} AND is_active = TRUE;

-- 期望结果：
-- | user_id | assign_type |
-- |---------|-------------|
-- | 1001    | ASSIGNED    |
-- | 1002    | CLAIMED     |
```

### 6.2 重新激活被删除的执行者

```bash
# 步骤1：移除用户A
PUT /api/tasks/{taskId}/assign
{"assigneeIds": []}

# 验证用户A被软删除
SELECT is_active FROM task_user WHERE task_id={taskId} AND user_id=1001;
-- 期望: FALSE

# 步骤2：重新分配用户A
PUT /api/tasks/{taskId}/assign
{"assigneeIds": [1001]}

# 验证记录被重新激活（而非创建新记录）
SELECT 
    id,
    user_id,
    is_active,
    removed_at,
    assigned_at
FROM task_user 
WHERE task_id = {taskId} AND user_id = 1001
ORDER BY assigned_at DESC 
LIMIT 1;

-- 期望结果：
-- - id不变（同一条记录）
-- - is_active = TRUE
-- - removed_at = NULL
-- - assigned_at更新为新时间
```

---

## 测试场景7：唯一约束测试

### 7.1 尝试重复分配

```sql
-- 手动尝试插入重复记录
INSERT INTO task_user (task_id, user_id, project_id, assign_type, assigned_by, role_type)
VALUES (123, 1001, 1, 'ASSIGNED', 2001, 'EXECUTOR');

-- 期望：违反唯一约束 uk_task_user_role
-- ERROR 1062 (23000): Duplicate entry '123-1001-EXECUTOR' for key 'uk_task_user_role'
```

---

## 测试场景8：级联删除测试

### 8.1 删除任务

```bash
DELETE /api/tasks/{taskId}
```

### 8.2 验证task_user记录被级联删除

```sql
SELECT COUNT(*) FROM task_user WHERE task_id = {taskId};
-- 期望: 0 (所有记录被物理删除)
```

---

## 🎯 性能基准

| 操作 | 预期时间 | 说明 |
|------|---------|------|
| 创建任务+分配2人 | < 100ms | 包含2次INSERT |
| 重新分配任务 | < 150ms | 包含软删除+新增 |
| 接取任务 | < 50ms | 单条INSERT |
| 查询任务详情 | < 80ms | 包含JOIN查询 |
| 批量查询50个任务 | < 200ms | 优化批量查询，避免N+1 |

---

## 🔍 常见问题排查

### 问题1：task_user表没有数据

**检查项**：
```sql
-- 1. 确认表存在
SHOW TABLES LIKE 'task_user';

-- 2. 确认外键约束存在
SHOW CREATE TABLE task_user;

-- 3. 查看最近的任务操作日志
SELECT * FROM tasks ORDER BY created_at DESC LIMIT 5;
```

### 问题2：assigneeId字段仍有数据

**解决方案**：
```sql
-- 这是废弃字段，应该都是 '[]'
UPDATE tasks SET assignee_id = '[]' WHERE assignee_id != '[]';
```

### 问题3：重复接取没有被阻止

**检查项**：
```java
// TaskServiceImpl.claimTask() 方法中应有这个检查
if (taskUserRepository.isUserActiveExecutor(taskId, userId)) {
    throw new IllegalArgumentException("您已经是该任务的执行者");
}
```

### 问题4：查询性能慢

**优化检查**：
```sql
-- 验证索引存在
SHOW INDEX FROM task_user;

-- 应该有以下索引：
-- idx_task_id
-- idx_user_id
-- idx_user_project
-- idx_task_user_active
-- idx_user_active
-- idx_assigned_at
```

---

## ✅ 测试检查清单

- [ ] **测试1**：创建任务时task_user表有记录
- [ ] **测试2**：分配任务时旧记录被软删除，新记录被添加
- [ ] **测试3**：接取任务时assign_type=CLAIMED
- [ ] **测试4**：重复接取被阻止
- [ ] **测试5**：查询任务时从task_user表读取
- [ ] **测试6**：批量查询避免了N+1问题
- [ ] **测试7**：重新激活时使用同一条记录而非新建
- [ ] **测试8**：唯一约束正常工作
- [ ] **测试9**：级联删除正常工作
- [ ] **测试10**：assignType和assignedAt字段正确返回

---

## 📊 监控指标

建议在生产环境监控以下指标：

1. **task_user表增长速率**
   ```sql
   SELECT COUNT(*) FROM task_user WHERE created_at > NOW() - INTERVAL 1 DAY;
   ```

2. **软删除比例**
   ```sql
   SELECT 
       SUM(CASE WHEN is_active = FALSE THEN 1 ELSE 0 END) / COUNT(*) * 100 as soft_delete_percent
   FROM task_user;
   ```

3. **CLAIMED vs ASSIGNED比例**
   ```sql
   SELECT 
       assign_type,
       COUNT(*) as count,
       COUNT(*) * 100.0 / SUM(COUNT(*)) OVER () as percentage
   FROM task_user
   WHERE is_active = TRUE
   GROUP BY assign_type;
   ```

---

## 📝 测试报告模板

```
测试时间: 2025-11-04
测试人员: [姓名]
环境: [开发/测试/生产]

| 测试项 | 预期结果 | 实际结果 | 状态 |
|--------|---------|---------|------|
| 创建任务写入task_user | 成功 | 成功 | ✅ |
| 分配任务软删除+新增 | 成功 | 成功 | ✅ |
| 接取任务CLAIMED类型 | 成功 | 成功 | ✅ |
| 查询从task_user读取 | 成功 | 成功 | ✅ |
| N+1问题已解决 | 成功 | 成功 | ✅ |
| assignType字段显示 | 成功 | 成功 | ✅ |

总结: 所有测试通过 ✅
```

---

## 🔗 相关文档

- [修复说明文档](./任务分配接取功能修复说明-v2.md)
- [task_user表DDL](../sql/zhiyan-mysql-service/task_user_relation.sql)
- [数据迁移脚本](../sql/zhiyan-mysql-service/MIGRON/V2.1__create_task_user_table.sql)
- [TaskUser实体](../zhiyan-modules/zhiyan-project/src/main/java/hbnu/project/zhiyanproject/model/entity/TaskUser.java)
- [TaskUserRepository](../zhiyan-modules/zhiyan-project/src/main/java/hbnu/project/zhiyanproject/repository/TaskUserRepository.java)

