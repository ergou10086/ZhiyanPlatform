# Bootstrap配置文件 - 最先加载的配置
# 用于配置应用名称、配置中心等基础信息

spring:
  application:
    name: zhiyan-wiki
  profiles:
    # 默认激活的环境：dev(开发环境) / prod(生产环境)
    # 可通过启动参数覆盖：--spring.profiles.active=prod
    active: ${SPRING_PROFILES_ACTIVE:prod}
  cloud:
    # 如果使用Nacos配置中心，请取消注释并配置
    # nacos:
    #   discovery:
    #     enabled: true
    #   config:
    #     enabled: true
    #     server-addr: ${NACOS_SERVER_ADDR:localhost:8848}
    #     namespace: ${NACOS_NAMESPACE:}
    #     group: DEFAULT_GROUP
    #     file-extension: yaml
    #     refresh-enabled: true

# 应用基本信息
app:
  name: 智研平台 - Wiki知识库模块
  version: 0.0.1-SNAPSHOT
  description: 提供项目Wiki知识库管理功能
