# 任务功能完整替换指南

## 📋 概述

本指南将帮助你在 `ProjectDetail.vue` 中用**新的任务提交审核功能**完全替换旧的任务结果功能。

---

## 🎯 替换目标

### 移除的旧功能
- ❌ 任务结果上传（简单的文字+图片）
- ❌ `/api/projects/tasks/{id}/result` 接口调用
- ❌ `uploadTaskResult` 相关方法
- ❌ `loadTaskResult` 相关方法

### 添加的新功能
- ✅ 任务提交（完整的提交说明+附件+工时）
- ✅ 任务提交审核流程
- ✅ 提交历史查看
- ✅ 审核状态追踪
- ✅ `/api/projects/tasks/submissions/*` 接口

---

## 🚀 替换步骤

### 步骤 1: 在 script 部分添加导入

在 `ProjectDetail.vue` 的 `<script>` 标签开头添加：

```javascript
// 在文件顶部导入任务提交相关组件和API
import TaskSubmissionModal from '@/components/TaskSubmissionModal.vue'
import TaskSubmissionReviewModal from '@/components/TaskSubmissionReviewModal.vue'
import { getTaskSubmissions, getLatestSubmission } from '@/api/taskSubmission'
```

---

### 步骤 2: 注册组件

在 `components` 对象中添加：

```javascript
export default {
  name: 'ProjectDetail',
  components: {
    TaskSubmissionModal,          // 添加
    TaskSubmissionReviewModal     // 添加
  },
  // ...
}
```

---

### 步骤 3: 修改 data() 数据属性

找到 `data()` 方法，**删除或注释掉**旧的任务结果相关数据：

```javascript
data() {
  return {
    // ... 其他现有数据 ...
    
    // ===== 删除或注释以下旧的任务结果数据 =====
    // uploadResultModalOpen: false,
    // taskToUploadResult: null,
    // resultText: '',
    // resultImages: [],
    // isUploadingResult: false,
    
    // ===== 添加新的任务提交数据 =====
    // 任务提交相关
    taskSubmissionModalVisible: false,
    taskToSubmit: null,
    taskSubmissions: [],
    
    // 任务审核相关
    taskReviewModalVisible: false,
    submissionToReview: null,
  }
}
```

---

### 步骤 4: 删除或注释旧的方法

在 `methods` 对象中，**删除或注释掉**以下方法：

```javascript
methods: {
  // ... 其他方法 ...
  
  // ===== 删除或注释以下方法 =====
  /*
  openUploadResultModal(task) { ... },
  closeUploadResultModal() { ... },
  loadTaskResult(taskId) { ... },
  handleImageSelect(event) { ... },
  removeImage(index) { ... },
  confirmUploadResult() { ... },
  */
}
```

---

### 步骤 5: 添加新的方法

在 `methods` 对象中添加以下方法：

```javascript
methods: {
  // ... 其他现有方法 ...
  
  // ==================== 任务提交相关方法 ====================
  
  /**
   * 打开任务提交弹窗
   */
  openTaskSubmissionModal(task) {
    // 检查是否为任务执行者
    const currentUserId = this.getCurrentUserId()
    const isAssignee = this.isTaskAssignee(task, currentUserId)
    
    if (!isAssignee) {
      this.$message.warning('只有任务执行者才能提交任务')
      return
    }
    
    // 检查任务状态
    if (task.status === 'DONE') {
      this.$message.info('任务已完成，无需重复提交')
      return
    }
    
    this.taskToSubmit = task
    this.taskSubmissionModalVisible = true
    
    // 加载任务提交历史
    this.loadTaskSubmissions(task.id)
  },
  
  /**
   * 关闭任务提交弹窗
   */
  closeTaskSubmissionModal() {
    this.taskSubmissionModalVisible = false
    this.taskToSubmit = null
  },
  
  /**
   * 任务提交成功回调
   */
  handleTaskSubmitSuccess(submission) {
    console.log('任务提交成功:', submission)
    this.$message.success('任务提交成功，等待审核')
    
    // 刷新任务列表
    this.loadTasks()
    
    // 关闭弹窗
    this.closeTaskSubmissionModal()
  },
  
  /**
   * 加载任务提交历史
   */
  async loadTaskSubmissions(taskId) {
    try {
      const response = await getTaskSubmissions(taskId)
      if (response.code === 200) {
        this.taskSubmissions = response.data || []
        console.log('任务提交历史:', this.taskSubmissions)
      }
    } catch (error) {
      console.error('加载任务提交历史失败', error)
    }
  },
  
  /**
   * 检查当前用户是否为任务执行者
   */
  isTaskAssignee(task, userId) {
    if (!task || !userId) return false
    
    // 检查 assignee_ids 数组
    if (task.assignee_ids && Array.isArray(task.assignee_ids)) {
      return task.assignee_ids.includes(userId) || 
             task.assignee_ids.includes(String(userId))
    }
    
    // 检查 assigneeIds 数组（备用字段名）
    if (task.assigneeIds && Array.isArray(task.assigneeIds)) {
      return task.assigneeIds.includes(userId) || 
             task.assigneeIds.includes(String(userId))
    }
    
    return false
  },
  
  /**
   * 从任务详情弹窗触发提交
   */
  handleSubmitTask(task) {
    // 关闭任务详情弹窗
    this.closeTaskDetailModal()
    
    // 打开任务提交弹窗
    this.openTaskSubmissionModal(task)
  },
  
  // ==================== 任务审核相关方法 ====================
  
  /**
   * 打开任务审核弹窗（项目负责人/任务创建者可用）
   */
  async openTaskReviewModal(task) {
    // 检查是否有审核权限
    if (!this.isProjectManager && task.created_by !== this.getCurrentUserId()) {
      this.$message.warning('只有项目负责人或任务创建者才能审核提交')
      return
    }
    
    try {
      // 获取任务的最新提交
      const response = await getLatestSubmission(task.id)
      if (response.code === 200 && response.data) {
        const submission = response.data
        
        // 检查提交状态
        if (submission.reviewStatus !== 'PENDING') {
          this.$message.info('该提交已审核')
          return
        }
        
        this.submissionToReview = submission
        this.taskReviewModalVisible = true
      } else {
        this.$message.info('该任务暂无待审核的提交')
      }
    } catch (error) {
      console.error('获取待审核提交失败', error)
      this.$message.error('获取待审核提交失败')
    }
  },
  
  /**
   * 关闭审核弹窗
   */
  closeTaskReviewModal() {
    this.taskReviewModalVisible = false
    this.submissionToReview = null
  },
  
  /**
   * 审核成功回调
   */
  handleReviewSuccess(submission) {
    console.log('审核完成:', submission)
    this.$message.success('审核完成')
    
    // 刷新任务列表
    this.loadTasks()
    
    // 关闭弹窗
    this.closeTaskReviewModal()
  },
  
  /**
   * 查看任务提交历史
   */
  async viewTaskSubmissions(task) {
    try {
      const response = await getTaskSubmissions(task.id)
      if (response.code === 200) {
        const submissions = response.data || []
        
        if (submissions.length === 0) {
          this.$message.info('该任务暂无提交记录')
          return
        }
        
        // 显示提交历史（可以用弹窗或跳转到详情页）
        console.log('提交历史:', submissions)
        this.$message.success(`该任务共有 ${submissions.length} 条提交记录`)
        
        // TODO: 可以创建一个提交历史查看弹窗
      }
    } catch (error) {
      console.error('获取提交历史失败', error)
      this.$message.error('获取提交历史失败')
    }
  },
  
  // ==================== 辅助方法 ====================
  
  /**
   * 获取当前用户ID
   */
  getCurrentUserId() {
    const userInfo = JSON.parse(localStorage.getItem('user_info') || '{}')
    return userInfo.id || userInfo.userId || null
  }
}
```

---

### 步骤 6: 修改任务详情弹窗底部按钮

找到任务详情弹窗的 `modal-footer` 部分（大约在第850行），**替换**为：

```html
<div class="modal-footer">
  <!-- 任务执行者：显示提交任务按钮 -->
  <button 
    v-if="isTaskAssignee(selectedTask, getCurrentUserId()) && selectedTask.status !== 'DONE'" 
    @click="handleSubmitTask(selectedTask)" 
    class="btn btn-submit"
  >
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M9 11L12 14L22 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      <path d="M21 12V19C21 19.5304 20.7893 20.0391 20.4142 20.4142C20.0391 20.7893 19.5304 21 19 21H5C4.46957 21 3.96086 20.7893 3.58579 20.4142C3.21071 20.0391 3 19.5304 3 19V5C3 4.46957 3.21071 3.96086 3.58579 3.58579C3.96086 3.21071 4.46957 3 5 3H16" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
    提交任务
  </button>
  
  <!-- 项目负责人/创建者：显示审核按钮 -->
  <button 
    v-if="(isProjectManager || selectedTask.created_by === getCurrentUserId())" 
    @click="openTaskReviewModal(selectedTask)" 
    class="btn btn-review"
  >
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M9 11L12 14L22 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
    审核提交
  </button>
  
  <!-- 查看提交历史 -->
  <button 
    @click="viewTaskSubmissions(selectedTask)" 
    class="btn btn-secondary"
  >
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M12 22C17.5228 22 22 17.5228 22 12C22 6.47715 17.5228 2 12 2C6.47715 2 2 6.47715 2 12C2 17.5228 6.47715 22 12 22Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      <path d="M12 6V12L16 14" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
    提交历史
  </button>
  
  <!-- 关闭按钮 -->
  <button @click="closeTaskDetailModal" class="btn btn-secondary">关闭</button>
</div>
```

---

### 步骤 7: 移除旧的任务结果上传弹窗模板

在 `<template>` 部分，找到并**删除或注释掉**旧的任务结果上传弹窗：

```html
<!-- ===== 删除或注释这整个弹窗 ===== -->
<!--
<div v-if="uploadResultModalOpen" class="modal-overlay" @click="closeUploadResultModal">
  <div class="modal-content" @click.stop>
    ... 旧的任务结果上传内容 ...
  </div>
</div>
-->
```

---

### 步骤 8: 添加新的任务提交和审核弹窗

在 `<template>` 的**最底部**（在所有弹窗之后）添加：

```html
<!-- 任务提交弹窗 -->
<TaskSubmissionModal
  :visible.sync="taskSubmissionModalVisible"
  :task="taskToSubmit || {}"
  @close="closeTaskSubmissionModal"
  @success="handleTaskSubmitSuccess"
/>

<!-- 任务审核弹窗 -->
<TaskSubmissionReviewModal
  :visible.sync="taskReviewModalVisible"
  :submission="submissionToReview || {}"
  @close="closeTaskReviewModal"
  @success="handleReviewSuccess"
/>
```

---

### 步骤 9: 添加样式

在 `<style scoped>` 中添加按钮样式：

```css
/* 任务提交按钮 */
.btn-submit {
  background-color: #4CAF50;
  color: white;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 20px;
  border: none;
  border-radius: 6px;
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s;
}

.btn-submit:hover {
  background-color: #45a049;
}

/* 审核按钮 */
.btn-review {
  background-color: #2196F3;
  color: white;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 20px;
  border: none;
  border-radius: 6px;
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s;
}

.btn-review:hover {
  background-color: #1976D2;
}

/* 按钮图标 */
.btn-submit svg,
.btn-review svg {
  stroke-width: 2;
}
```

---

### 步骤 10: 清理旧代码（可选）

如果确认不再需要旧功能，可以完全删除：

1. 删除 `data()` 中的旧数据属性
2. 删除 `methods` 中的旧方法
3. 删除 `<template>` 中的旧弹窗
4. 删除相关的 CSS 样式

---

## ✅ 验证清单

完成替换后，检查以下项目：

- [ ] 导入了 `TaskSubmissionModal` 和 `TaskSubmissionReviewModal` 组件
- [ ] 导入了 `taskSubmission` API
- [ ] 注册了组件
- [ ] 添加了新的数据属性
- [ ] 添加了新的方法
- [ ] 修改了任务详情弹窗底部按钮
- [ ] 添加了新的弹窗组件到模板
- [ ] 删除或注释了旧的代码
- [ ] 添加了必要的样式
- [ ] 没有 linter 错误
- [ ] 页面可以正常加载

---

## 🧪 测试步骤

### 1. 测试任务提交

1. 打开项目详情页
2. 点击任务卡片，查看任务详情
3. 如果是任务执行者，应该看到"提交任务"按钮
4. 点击"提交任务"按钮
5. 填写提交说明（至少10字）
6. 可选：上传附件
7. 可选：填写实际工时
8. 选择提交类型
9. 勾选"这是最终提交"（如果任务已完成）
10. 点击"提交任务"按钮
11. 应该显示"任务提交成功，等待审核"消息

### 2. 测试任务审核

1. 以项目负责人身份登录
2. 打开项目详情页
3. 找到有待审核提交的任务
4. 点击任务详情
5. 点击"审核提交"按钮
6. 查看提交内容
7. 选择"批准通过"或"拒绝退回"
8. 可选：填写审核意见
9. 点击确认按钮
10. 应该显示审核成功消息

### 3. 测试提交历史

1. 打开任务详情
2. 点击"提交历史"按钮
3. 应该显示该任务的所有提交记录数量
4. 控制台应该打印提交历史数据

---

## 🐛 常见问题

### Q1: 看不到"提交任务"按钮？
**A**: 检查：
- 当前用户是否为任务执行者
- `isTaskAssignee` 方法是否正确判断
- 任务状态是否为 DONE

### Q2: 看不到"审核提交"按钮？
**A**: 检查：
- 当前用户是否为项目负责人
- 或当前用户是否为任务创建者
- `isProjectManager` 变量是否正确

### Q3: 点击按钮没反应？
**A**: 打开浏览器控制台，查看：
- 是否有 JavaScript 错误
- API 请求是否成功
- 响应数据是否正确

### Q4: 文件上传失败？
**A**: 检查：
- MinIO 服务是否运行
- `tasksubmission` 桶是否已创建
- 文件大小是否超过限制（100MB）

---

## 📚 相关文档

- `TaskSubmissionModal.vue` - 任务提交弹窗组件
- `TaskSubmissionReviewModal.vue` - 任务审核弹窗组件
- `TaskSubmissionReview.vue` - 审核列表页面
- `taskSubmission.js` - API 封装

---

## 🎉 完成！

恭喜！你已经成功用新的任务提交审核功能替换了旧的任务结果功能。

**新功能优势**：
- ✅ 完整的提交审核流程
- ✅ 支持多种文件类型附件
- ✅ 版本管理（支持多次提交）
- ✅ 审核状态追踪
- ✅ 权限控制
- ✅ 工时记录

**下一步建议**：
1. 添加路由到审核列表页面
2. 在导航栏添加"待审核"入口（带数量徽章）
3. 创建提交历史查看弹窗
4. 添加通知功能（提交后通知审核人）

---

祝你使用愉快！🚀

